import styled from "styled-components";

export const FooterContainerStyled = styled.footer`
    text-align: center;
    padding: 1rem 3rem;
    margin:1rem 0;
    footer img {
    display: block;
    margin: auto;
    }
    footer img + p {
        text-align: center;
    }
`